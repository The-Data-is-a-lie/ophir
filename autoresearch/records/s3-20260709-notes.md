# s3-20260709 session notes — contaminated rows (read before citing)

The s3 record spans four launch attempts on 2026-07-09; the machine's idle
power transition killed the first three (root cause + fixes: commits
`c6b8670`, `abfdb1b`, `424b98a`, `3b127a6`). Restarted attempts reused
iteration directories, and until `3b127a6` the loop's alphabetical
`ckpts[-1]` selection could score a **stale checkpoint left by a dead
attempt**. Verified contamination in this file's rows:

| row | value | status |
| --- | ----- | ------ |
| baseline 0.09635 (07:38Z) | clean | pre-rebalance store; matches recal |
| baseline 0.09289 (both) | **contaminated** | seeds 0/1 scored stale ckpts; only seed-2 (0.06527) genuine — true post-rebalance baseline is s4's 0.09369 |
| iter-1 "ablation" 0.06329 | **contaminated** | seed-0 (−0.00288) scored a 500-step half-trained leftover; seeds 1–2 genuine |
| iter-1 "EMA" 0.09597 (2nd run) | **contaminated** | seeds 1–2 are bit-identical copies of the ablation row (scored its leftovers); only seed-0 (0.09515) fresh |

Clean, superseding measurements live in `s4-20260709-results.tsv`
(fresh dirs + purge fix): baseline 0.09369, aux-head ablation 0.10213,
bias-corrected EMA-0.9995 0.09114 — both trials discarded vs bar 0.14269.

Bit-identical seed values across runs are EXPECTED for identical code+data
(deterministic harness) — identical values across *different* trials are the
contamination signature that exposed this.
